apiVersion: v1
kind: Service
metadata:
  name: quote-app
  namespace: quote-lab
spec:
  selector:
    app: quote-app
  ports:
    - port: 80
      targetPort: 3232
      nodePort: 30080
  type: NodePort